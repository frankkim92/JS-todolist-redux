import React from "react";

const Header = () => {
  const StyledHeader = { backgroundColor: "cornflowerblue", padding: "20px" };
  return (
    <div>
      <header style={StyledHeader}>헤더입니다.</header>
    </div>
  );
};

export default Header;
